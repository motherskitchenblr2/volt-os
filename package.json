{
  "name": "volt-os",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "vitest": "^2.1.9",
    "typescript": "^5.9.3",
    "@types/node": "^22.20.0"
  }
}
